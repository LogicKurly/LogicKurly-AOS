package com.kurly.logickurly.presentation.base

import androidx.lifecycle.ViewModel

abstract class BaseViewModel: ViewModel() {
    // ViewModel단에서는 화면에서 사용하는 데이터의 변수만을 담고 있는 역할을 하기 때문에 아직 별다른 역할을 하지는 않습니다.
}